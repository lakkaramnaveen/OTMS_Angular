//it is a class same as User.java
//it should have same fields as User.java
export class User{
     userId:number;
     userName:string;
     userTest:number;
     isAdmin:boolean;
     userPassword:string; //bean class
}